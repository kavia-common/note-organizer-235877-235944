# Required: absolute path to SQLite database file (provided by database container)
SQLITE_DB=/absolute/path/to/myapp.db

# Optional: CORS origins for frontend. Use "*" for dev or a comma-separated list.
# Example: http://localhost:3000,https://your-domain.com
CORS_ALLOW_ORIGINS=*
